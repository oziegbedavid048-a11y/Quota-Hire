# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
__pycache__/
*.py[cod]
*$py.class
db.sqlite3

# Env
.env
.env.production
.env.local
.env.*.local
backend/.env

# Security audit working documents — kept local.
# This repository is public and these describe the application's
# vulnerabilities in detail, including one finding (the Firebase API key
# restriction) that is resolved in the Google Cloud console rather than in
# code. Move them to a private repo or a password manager if you need to
# share them.
SECURITY_AUDIT.html
SECURITY_FIXES_DEPLOY.md

# Build artifacts and stray captures at the repository root
build_output.txt
/IMG*.jpg

# Security audit artefacts — keep local, do not publish to a public repo
SECURITY_AUDIT.html
SECURITY_FIXES_DEPLOY.md

# Build artefacts
build_output.txt
build.zip
