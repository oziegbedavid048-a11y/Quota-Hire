Subject: Request to open Passive FTP Port range for GitHub Actions

Hello Smartweb Support,

I am trying to deploy my website via FTP using GitHub Actions, but my deployments keep failing with a data connection timeout error (e.g., "Timeout when trying to open data connection to ***:61572"). 

The FTP control connection on port 21 is successful, but the passive data connection is being blocked by the server's firewall. 

Could you please do the following to allow my GitHub Actions runner to connect?
1. Ensure that the Passive FTP Port Range (usually 49152 - 65534) is fully opened for incoming TCP connections in the server's firewall (CSF/LFD).
2. Ensure that the FTP server (Pure-FTPd or ProFTPD) is properly configured with the PassivePortRange and is returning the correct public IP address of the server, rather than an internal IP, during passive mode connections.

Since GitHub Actions uses dynamic Microsoft Azure IP ranges, whitelisting a single IP won't work. The passive port range needs to be universally accessible so the data transfer can complete. 

Thank you!
