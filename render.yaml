services:
  - type: web
    name: quotahire-backend
    env: python
    region: ohio
    plan: free
    rootDir: backend
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
    startCommand: "gunicorn quotahire.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --worker-class gthread --threads 2 --timeout 60 --max-requests 1000 --max-requests-jitter 100"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "quotahire-backend.onrender.com,localhost"
      - key: SECRET_KEY
        generateValue: true
      - key: FRONTEND_URL
        value: "https://quotahire.org"
      - key: CORS_ALLOWED_ORIGINS
        value: "https://quotahire.co.uk,https://www.quotahire.co.uk,https://quotahire.ng,https://www.quotahire.ng,https://quotahire.org,https://www.quotahire.org,http://quotahire.org,https://oziegbedavid048-a11y.github.io,http://localhost:5173"
      - key: CSRF_TRUSTED_ORIGINS
        value: "https://quotahire-backend.onrender.com,https://quotahire.co.uk,https://www.quotahire.co.uk,https://quotahire.ng,https://www.quotahire.ng,https://quotahire.org,https://www.quotahire.org"
      - key: EMAIL_HOST
        value: "smtp.sender.net"
      - key: EMAIL_PORT
        value: "587"
      - key: EMAIL_USE_TLS
        value: "True"
      - key: EMAIL_HOST_USER
        value: "Quotahire Backend_dRl0LE"
      # EMAIL_HOST_PASSWORD must be set in Render Dashboard (secret)
      # ZEPTOMAIL_API_KEY: the ZeptoMailBackend REST API key — MUST be set in Render Dashboard
      # settings.py reads: ZEPTOMAIL_API_KEY = config('ZEPTOMAIL_API_KEY', default=config('EMAIL_HOST_PASSWORD', default=''))
      # If EMAIL_HOST_PASSWORD is already set to the ZeptoMail key in Render Dashboard, this is already covered.
      # Otherwise set ZEPTOMAIL_API_KEY explicitly. Either way, one of these two MUST be present.
      - key: DEFAULT_FROM_EMAIL
        value: "Quota Hire <support@quotahire.org>"
      # CELERY_TASK_ALWAYS_EAGER: runs Celery tasks synchronously in the web process.
      # This ensures emails are sent immediately without a separate Celery worker service.
      # Remove this if you add a dedicated Celery worker service on a paid plan.
      - key: CELERY_TASK_ALWAYS_EAGER
        value: "True"
      # ── Paystack Payment Gateway ──────────────────────────────────────────
      # IMPORTANT: Set these in the Render Dashboard → Environment → Secret Files
      # PAYSTACK_SECRET_KEY and PAYSTACK_PUBLIC_KEY must be added manually
      - key: CV_DOWNLOAD_FEE_EUR
        value: "1.50"
      - key: NGN_PER_EUR
        value: "1650"
      # DATABASE_URL must be set manually in Render Dashboard (contains secret credentials)
      # COURIER_AUTH_TOKEN must be set manually in Render Dashboard
      # PAYSTACK_SECRET_KEY must be set manually in Render Dashboard
      # PAYSTACK_PUBLIC_KEY must be set manually in Render Dashboard
      # CLOUDINARY_URL must be set manually in Render Dashboard (if using file uploads)

  - type: web
    name: quotahire-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: "https://quotahire-backend.onrender.com/api"
      - key: VITE_PAYSTACK_PUBLIC_KEY
        value: "pk_live_beef698343ec5dbd2937927772c726f907e0db9e"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    headers:
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
      - path: /images/*
        name: Cache-Control
        value: public, max-age=604800, stale-while-revalidate=86400
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate
